# django-learning
![img.png](img.png)


## django template
- `{{ var }}` to add variable
- `{% ... %}` to add logic such as for loop / if clause
- `{# ... #}` for comment
- `{{ var | 过滤器}}` used to modify the variable here
